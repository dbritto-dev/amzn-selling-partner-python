# Auto-generated by oagen. Do not edit.

"""``FinancesV0`` resource of Selling Partner API.

Auto-generated by oagen from the OpenAPI spec; do not edit by hand.
"""

from __future__ import annotations

import datetime
from collections.abc import AsyncIterator, Iterator
from typing import Any

from .._http import AsyncHttpClient, HttpClient, RateLimit, RequestOptions, apaginate, paginate, path_segment
from ..models import finances_v0

SERVICE = "finances_v0"


class FinancesV0Resource:
    """Synchronous ``FinancesV0`` resource."""

    __slots__ = ("_http",)

    def __init__(self, http: HttpClient) -> None:
        self._http = http

    def list_financial_event_groups(
        self,
        *,
        max_results_per_page: int | None = None,
        financial_event_group_started_before: datetime.datetime | None = None,
        financial_event_group_started_after: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v0.ListFinancialEventGroupsResponse:
        """Returns financial event groups for a given date range. Orders from the last 48 hours might not be included in financial events.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 30 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput can have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits).

        GET /finances/v0/financialEventGroups
        """
        params: dict[str, Any] = {
            "MaxResultsPerPage": max_results_per_page,
            "FinancialEventGroupStartedBefore": financial_event_group_started_before,
            "FinancialEventGroupStartedAfter": financial_event_group_started_after,
            "NextToken": next_token,
        }
        return self._http.request(
            "GET",
            "/finances/v0/financialEventGroups",
            operation="listFinancialEventGroups",
            service=SERVICE,
            params=params,
            response=finances_v0.ListFinancialEventGroupsResponse,
            error=finances_v0.ListFinancialEventGroupsResponse,
            rate_limit=RateLimit(0.5, 30),
            options=request_options,
        )

    def iter_list_financial_event_groups(
        self,
        *,
        max_results_per_page: int | None = None,
        financial_event_group_started_before: datetime.datetime | None = None,
        financial_event_group_started_after: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> Iterator[finances_v0.FinancialEventGroup]:
        """every item of ``payload.FinancialEventGroupList`` across pages of ``list_financial_event_groups``, following ``payload.NextToken`` -> ``next_token``."""
        return paginate(
            self.list_financial_event_groups,
            {
                "max_results_per_page": max_results_per_page,
                "financial_event_group_started_before": financial_event_group_started_before,
                "financial_event_group_started_after": financial_event_group_started_after,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payload", "financial_event_group_list"),
            token=("payload", "next_token"),
            token_param="next_token",
            wire_items=("payload", "FinancialEventGroupList"),
            wire_token=("payload", "NextToken"),
            drop_params_on_next=True,
        )

    def list_financial_event_group_financial_events(
        self,
        event_group_id: str,
        *,
        max_results_per_page: int | None = None,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v0.ListFinancialEventsResponse:
        """Returns all financial events for the specified financial event group. Orders from the last 48 hours might not be included in financial events.

        **Note:** Deferred events don't appear in the `listFinancialEventsByGroupId` response until they are released.

        **Note:** This operation only retrieves a group's data for the past two years. A request for data spanning more than two years produces an empty response.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 30 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput can have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits).

        GET /finances/v0/financialEventGroups/{eventGroupId}/financialEvents
        """
        params: dict[str, Any] = {
            "MaxResultsPerPage": max_results_per_page,
            "PostedAfter": posted_after,
            "PostedBefore": posted_before,
            "NextToken": next_token,
        }
        return self._http.request(
            "GET",
            f"/finances/v0/financialEventGroups/{path_segment(event_group_id)}/financialEvents",
            operation="listFinancialEventsByGroupId",
            service=SERVICE,
            params=params,
            response=finances_v0.ListFinancialEventsResponse,
            error=finances_v0.ListFinancialEventsResponse,
            rate_limit=RateLimit(0.5, 30),
            options=request_options,
        )

    def iter_list_financial_event_group_financial_events(
        self,
        event_group_id: str,
        *,
        max_results_per_page: int | None = None,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> Iterator[finances_v0.FinancialEvents]:
        """the ``payload.FinancialEvents`` object of every page of ``list_financial_event_group_financial_events``, following ``payload.NextToken`` -> ``next_token``."""
        return paginate(
            self.list_financial_event_group_financial_events,
            {
                "event_group_id": event_group_id,
                "max_results_per_page": max_results_per_page,
                "posted_after": posted_after,
                "posted_before": posted_before,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payload", "financial_events"),
            token=("payload", "next_token"),
            token_param="next_token",
            wire_items=("payload", "FinancialEvents"),
            wire_token=("payload", "NextToken"),
            drop_params_on_next=True,
            single=True,
        )

    def list_order_financial_events(
        self,
        order_id: str,
        *,
        max_results_per_page: int | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v0.ListFinancialEventsResponse:
        """Returns all financial events for the specified order. Orders from the last 48 hours might not be included in financial events.

        **Note:** Deferred events don't appear in the `listFinancialEventsByOrderId` response until they are released.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 30 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput can have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits).

        GET /finances/v0/orders/{orderId}/financialEvents
        """
        params: dict[str, Any] = {"MaxResultsPerPage": max_results_per_page, "NextToken": next_token}
        return self._http.request(
            "GET",
            f"/finances/v0/orders/{path_segment(order_id)}/financialEvents",
            operation="listFinancialEventsByOrderId",
            service=SERVICE,
            params=params,
            response=finances_v0.ListFinancialEventsResponse,
            error=finances_v0.ListFinancialEventsResponse,
            rate_limit=RateLimit(0.5, 30),
            options=request_options,
        )

    def iter_list_order_financial_events(
        self,
        order_id: str,
        *,
        max_results_per_page: int | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> Iterator[finances_v0.FinancialEvents]:
        """the ``payload.FinancialEvents`` object of every page of ``list_order_financial_events``, following ``payload.NextToken`` -> ``next_token``."""
        return paginate(
            self.list_order_financial_events,
            {
                "order_id": order_id,
                "max_results_per_page": max_results_per_page,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payload", "financial_events"),
            token=("payload", "next_token"),
            token_param="next_token",
            wire_items=("payload", "FinancialEvents"),
            wire_token=("payload", "NextToken"),
            drop_params_on_next=True,
            single=True,
        )

    def list_financial_events(
        self,
        *,
        max_results_per_page: int | None = None,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v0.ListFinancialEventsResponse:
        """Returns financial events for the specified data range. Orders from the last 48 hours might not be included in financial events.

        **Note:** in `ListFinancialEvents`, deferred events don't show up in responses until they are released.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 30 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput can have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits).

        GET /finances/v0/financialEvents
        """
        params: dict[str, Any] = {
            "MaxResultsPerPage": max_results_per_page,
            "PostedAfter": posted_after,
            "PostedBefore": posted_before,
            "NextToken": next_token,
        }
        return self._http.request(
            "GET",
            "/finances/v0/financialEvents",
            operation="listFinancialEvents",
            service=SERVICE,
            params=params,
            response=finances_v0.ListFinancialEventsResponse,
            error=finances_v0.ListFinancialEventsResponse,
            rate_limit=RateLimit(0.5, 30),
            options=request_options,
        )

    def iter_list_financial_events(
        self,
        *,
        max_results_per_page: int | None = None,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> Iterator[finances_v0.FinancialEvents]:
        """the ``payload.FinancialEvents`` object of every page of ``list_financial_events``, following ``payload.NextToken`` -> ``next_token``."""
        return paginate(
            self.list_financial_events,
            {
                "max_results_per_page": max_results_per_page,
                "posted_after": posted_after,
                "posted_before": posted_before,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payload", "financial_events"),
            token=("payload", "next_token"),
            token_param="next_token",
            wire_items=("payload", "FinancialEvents"),
            wire_token=("payload", "NextToken"),
            drop_params_on_next=True,
            single=True,
        )


class AsyncFinancesV0Resource:
    """Asynchronous ``FinancesV0`` resource."""

    __slots__ = ("_http",)

    def __init__(self, http: AsyncHttpClient) -> None:
        self._http = http

    async def list_financial_event_groups(
        self,
        *,
        max_results_per_page: int | None = None,
        financial_event_group_started_before: datetime.datetime | None = None,
        financial_event_group_started_after: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v0.ListFinancialEventGroupsResponse:
        """Returns financial event groups for a given date range. Orders from the last 48 hours might not be included in financial events.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 30 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput can have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits).

        GET /finances/v0/financialEventGroups
        """
        params: dict[str, Any] = {
            "MaxResultsPerPage": max_results_per_page,
            "FinancialEventGroupStartedBefore": financial_event_group_started_before,
            "FinancialEventGroupStartedAfter": financial_event_group_started_after,
            "NextToken": next_token,
        }
        return await self._http.request(
            "GET",
            "/finances/v0/financialEventGroups",
            operation="listFinancialEventGroups",
            service=SERVICE,
            params=params,
            response=finances_v0.ListFinancialEventGroupsResponse,
            error=finances_v0.ListFinancialEventGroupsResponse,
            rate_limit=RateLimit(0.5, 30),
            options=request_options,
        )

    def iter_list_financial_event_groups(
        self,
        *,
        max_results_per_page: int | None = None,
        financial_event_group_started_before: datetime.datetime | None = None,
        financial_event_group_started_after: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[finances_v0.FinancialEventGroup]:
        """every item of ``payload.FinancialEventGroupList`` across pages of ``list_financial_event_groups``, following ``payload.NextToken`` -> ``next_token``."""
        return apaginate(
            self.list_financial_event_groups,
            {
                "max_results_per_page": max_results_per_page,
                "financial_event_group_started_before": financial_event_group_started_before,
                "financial_event_group_started_after": financial_event_group_started_after,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payload", "financial_event_group_list"),
            token=("payload", "next_token"),
            token_param="next_token",
            wire_items=("payload", "FinancialEventGroupList"),
            wire_token=("payload", "NextToken"),
            drop_params_on_next=True,
        )

    async def list_financial_event_group_financial_events(
        self,
        event_group_id: str,
        *,
        max_results_per_page: int | None = None,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v0.ListFinancialEventsResponse:
        """Returns all financial events for the specified financial event group. Orders from the last 48 hours might not be included in financial events.

        **Note:** Deferred events don't appear in the `listFinancialEventsByGroupId` response until they are released.

        **Note:** This operation only retrieves a group's data for the past two years. A request for data spanning more than two years produces an empty response.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 30 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput can have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits).

        GET /finances/v0/financialEventGroups/{eventGroupId}/financialEvents
        """
        params: dict[str, Any] = {
            "MaxResultsPerPage": max_results_per_page,
            "PostedAfter": posted_after,
            "PostedBefore": posted_before,
            "NextToken": next_token,
        }
        return await self._http.request(
            "GET",
            f"/finances/v0/financialEventGroups/{path_segment(event_group_id)}/financialEvents",
            operation="listFinancialEventsByGroupId",
            service=SERVICE,
            params=params,
            response=finances_v0.ListFinancialEventsResponse,
            error=finances_v0.ListFinancialEventsResponse,
            rate_limit=RateLimit(0.5, 30),
            options=request_options,
        )

    def iter_list_financial_event_group_financial_events(
        self,
        event_group_id: str,
        *,
        max_results_per_page: int | None = None,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[finances_v0.FinancialEvents]:
        """the ``payload.FinancialEvents`` object of every page of ``list_financial_event_group_financial_events``, following ``payload.NextToken`` -> ``next_token``."""
        return apaginate(
            self.list_financial_event_group_financial_events,
            {
                "event_group_id": event_group_id,
                "max_results_per_page": max_results_per_page,
                "posted_after": posted_after,
                "posted_before": posted_before,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payload", "financial_events"),
            token=("payload", "next_token"),
            token_param="next_token",
            wire_items=("payload", "FinancialEvents"),
            wire_token=("payload", "NextToken"),
            drop_params_on_next=True,
            single=True,
        )

    async def list_order_financial_events(
        self,
        order_id: str,
        *,
        max_results_per_page: int | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v0.ListFinancialEventsResponse:
        """Returns all financial events for the specified order. Orders from the last 48 hours might not be included in financial events.

        **Note:** Deferred events don't appear in the `listFinancialEventsByOrderId` response until they are released.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 30 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput can have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits).

        GET /finances/v0/orders/{orderId}/financialEvents
        """
        params: dict[str, Any] = {"MaxResultsPerPage": max_results_per_page, "NextToken": next_token}
        return await self._http.request(
            "GET",
            f"/finances/v0/orders/{path_segment(order_id)}/financialEvents",
            operation="listFinancialEventsByOrderId",
            service=SERVICE,
            params=params,
            response=finances_v0.ListFinancialEventsResponse,
            error=finances_v0.ListFinancialEventsResponse,
            rate_limit=RateLimit(0.5, 30),
            options=request_options,
        )

    def iter_list_order_financial_events(
        self,
        order_id: str,
        *,
        max_results_per_page: int | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[finances_v0.FinancialEvents]:
        """the ``payload.FinancialEvents`` object of every page of ``list_order_financial_events``, following ``payload.NextToken`` -> ``next_token``."""
        return apaginate(
            self.list_order_financial_events,
            {
                "order_id": order_id,
                "max_results_per_page": max_results_per_page,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payload", "financial_events"),
            token=("payload", "next_token"),
            token_param="next_token",
            wire_items=("payload", "FinancialEvents"),
            wire_token=("payload", "NextToken"),
            drop_params_on_next=True,
            single=True,
        )

    async def list_financial_events(
        self,
        *,
        max_results_per_page: int | None = None,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v0.ListFinancialEventsResponse:
        """Returns financial events for the specified data range. Orders from the last 48 hours might not be included in financial events.

        **Note:** in `ListFinancialEvents`, deferred events don't show up in responses until they are released.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 30 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput can have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits).

        GET /finances/v0/financialEvents
        """
        params: dict[str, Any] = {
            "MaxResultsPerPage": max_results_per_page,
            "PostedAfter": posted_after,
            "PostedBefore": posted_before,
            "NextToken": next_token,
        }
        return await self._http.request(
            "GET",
            "/finances/v0/financialEvents",
            operation="listFinancialEvents",
            service=SERVICE,
            params=params,
            response=finances_v0.ListFinancialEventsResponse,
            error=finances_v0.ListFinancialEventsResponse,
            rate_limit=RateLimit(0.5, 30),
            options=request_options,
        )

    def iter_list_financial_events(
        self,
        *,
        max_results_per_page: int | None = None,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[finances_v0.FinancialEvents]:
        """the ``payload.FinancialEvents`` object of every page of ``list_financial_events``, following ``payload.NextToken`` -> ``next_token``."""
        return apaginate(
            self.list_financial_events,
            {
                "max_results_per_page": max_results_per_page,
                "posted_after": posted_after,
                "posted_before": posted_before,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payload", "financial_events"),
            token=("payload", "next_token"),
            token_param="next_token",
            wire_items=("payload", "FinancialEvents"),
            wire_token=("payload", "NextToken"),
            drop_params_on_next=True,
            single=True,
        )


__all__ = ["AsyncFinancesV0Resource", "FinancesV0Resource"]
