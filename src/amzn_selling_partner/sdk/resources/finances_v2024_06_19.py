# Auto-generated by oagen. Do not edit.

"""``FinancesV20240619`` resource of Selling Partner API.

Auto-generated by oagen from the OpenAPI spec; do not edit by hand.
"""

from __future__ import annotations

import datetime
from collections.abc import AsyncIterator, Iterator
from typing import Any

from .._http import AsyncHttpClient, HttpClient, RateLimit, RequestOptions, apaginate, joined, paginate
from ..models import finances_v2024_06_19

SERVICE = "finances_v2024_06_19"


class FinancesV20240619Resource:
    """Synchronous ``FinancesV20240619`` resource."""

    __slots__ = ("_http",)

    def __init__(self, http: HttpClient) -> None:
        self._http = http

    def list_transactions(
        self,
        *,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        marketplace_id: str | None = None,
        transaction_status: str | None = None,
        related_identifier_name: str | None = None,
        related_identifier_value: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v2024_06_19.ListTransactionsResponse:
        """Returns transactions for the given parameters. Financial events might not include orders from the last 48 hours. **Note:** If you want to retrieve MFN orders in the US store, do not supply the `marketplaceId` parameter.

        **Usage plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 10 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits).

        GET /finances/2024-06-19/transactions
        """
        params: dict[str, Any] = {
            "postedAfter": posted_after,
            "postedBefore": posted_before,
            "marketplaceId": marketplace_id,
            "transactionStatus": transaction_status,
            "relatedIdentifierName": related_identifier_name,
            "relatedIdentifierValue": related_identifier_value,
            "nextToken": next_token,
        }
        return self._http.request(
            "GET",
            "/finances/2024-06-19/transactions",
            operation="listTransactions",
            service=SERVICE,
            params=params,
            response=finances_v2024_06_19.ListTransactionsResponse,
            error=finances_v2024_06_19.ErrorList,
            rate_limit=RateLimit(0.5, 10),
            options=request_options,
        )

    def iter_list_transactions(
        self,
        *,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        marketplace_id: str | None = None,
        transaction_status: str | None = None,
        related_identifier_name: str | None = None,
        related_identifier_value: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> Iterator[finances_v2024_06_19.Transaction]:
        """every item of ``payload.transactions`` across pages of ``list_transactions``, following ``payload.nextToken`` -> ``next_token``."""
        return paginate(
            self.list_transactions,
            {
                "posted_after": posted_after,
                "posted_before": posted_before,
                "marketplace_id": marketplace_id,
                "transaction_status": transaction_status,
                "related_identifier_name": related_identifier_name,
                "related_identifier_value": related_identifier_value,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payload", "transactions"),
            token=("payload", "next_token"),
            token_param="next_token",
            wire_items=("payload", "transactions"),
            wire_token=("payload", "nextToken"),
        )

    def list_balances(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        balance_type: str | None = None,
        account_type: str | None = None,
        as_of_date: datetime.date | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v2024_06_19.ListBalancesResponse:
        """Retrieve a balance. This balance can be a past balances at a specified date, or a current balance. Sub-balances are listed by account type and marketplace.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 10 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that apply to the operation, when available. The preceding table indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        GET /finances/2024-06-19/balances
        """
        params: dict[str, Any] = {
            "marketplaceIds": joined(marketplace_ids, ","),
            "balanceType": balance_type,
            "accountType": account_type,
            "asOfDate": as_of_date,
            "nextToken": next_token,
        }
        return self._http.request(
            "GET",
            "/finances/2024-06-19/balances",
            operation="listBalances",
            service=SERVICE,
            params=params,
            response=finances_v2024_06_19.ListBalancesResponse,
            error=finances_v2024_06_19.ErrorList,
            rate_limit=RateLimit(0.5, 10),
            options=request_options,
        )

    def iter_list_balances(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        balance_type: str | None = None,
        account_type: str | None = None,
        as_of_date: datetime.date | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> Iterator[finances_v2024_06_19.Balance]:
        """every item of ``balances`` across pages of ``list_balances``, following ``nextToken`` -> ``next_token``."""
        return paginate(
            self.list_balances,
            {
                "marketplace_ids": marketplace_ids,
                "balance_type": balance_type,
                "account_type": account_type,
                "as_of_date": as_of_date,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("balances",),
            token=("next_token",),
            token_param="next_token",
            wire_items=("balances",),
            wire_token=("nextToken",),
        )

    def list_summary(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        account_type: str | None = None,
        related_identifier_name: str | None = None,
        related_identifier_value: str | None = None,
        period_start: datetime.date | None = None,
        period_end: datetime.date | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v2024_06_19.SummaryResponse:
        """Retrieve the financial summary for the specified time period or settlement period.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 10 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that apply to the operation, when available. The preceding table indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        GET /finances/2024-06-19/summary
        """
        params: dict[str, Any] = {
            "marketplaceIds": joined(marketplace_ids, ","),
            "accountType": account_type,
            "relatedIdentifierName": related_identifier_name,
            "relatedIdentifierValue": related_identifier_value,
            "periodStart": period_start,
            "periodEnd": period_end,
            "nextToken": next_token,
        }
        return self._http.request(
            "GET",
            "/finances/2024-06-19/summary",
            operation="listSummary",
            service=SERVICE,
            params=params,
            response=finances_v2024_06_19.SummaryResponse,
            error=finances_v2024_06_19.ErrorList,
            rate_limit=RateLimit(0.5, 10),
            options=request_options,
        )

    def iter_list_summary(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        account_type: str | None = None,
        related_identifier_name: str | None = None,
        related_identifier_value: str | None = None,
        period_start: datetime.date | None = None,
        period_end: datetime.date | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> Iterator[finances_v2024_06_19.Summary]:
        """every item of ``summaries`` across pages of ``list_summary``, following ``nextToken`` -> ``next_token``."""
        return paginate(
            self.list_summary,
            {
                "marketplace_ids": marketplace_ids,
                "account_type": account_type,
                "related_identifier_name": related_identifier_name,
                "related_identifier_value": related_identifier_value,
                "period_start": period_start,
                "period_end": period_end,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("summaries",),
            token=("next_token",),
            token_param="next_token",
            wire_items=("summaries",),
            wire_token=("nextToken",),
        )


class AsyncFinancesV20240619Resource:
    """Asynchronous ``FinancesV20240619`` resource."""

    __slots__ = ("_http",)

    def __init__(self, http: AsyncHttpClient) -> None:
        self._http = http

    async def list_transactions(
        self,
        *,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        marketplace_id: str | None = None,
        transaction_status: str | None = None,
        related_identifier_name: str | None = None,
        related_identifier_value: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v2024_06_19.ListTransactionsResponse:
        """Returns transactions for the given parameters. Financial events might not include orders from the last 48 hours. **Note:** If you want to retrieve MFN orders in the US store, do not supply the `marketplaceId` parameter.

        **Usage plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 10 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits).

        GET /finances/2024-06-19/transactions
        """
        params: dict[str, Any] = {
            "postedAfter": posted_after,
            "postedBefore": posted_before,
            "marketplaceId": marketplace_id,
            "transactionStatus": transaction_status,
            "relatedIdentifierName": related_identifier_name,
            "relatedIdentifierValue": related_identifier_value,
            "nextToken": next_token,
        }
        return await self._http.request(
            "GET",
            "/finances/2024-06-19/transactions",
            operation="listTransactions",
            service=SERVICE,
            params=params,
            response=finances_v2024_06_19.ListTransactionsResponse,
            error=finances_v2024_06_19.ErrorList,
            rate_limit=RateLimit(0.5, 10),
            options=request_options,
        )

    def iter_list_transactions(
        self,
        *,
        posted_after: datetime.datetime | None = None,
        posted_before: datetime.datetime | None = None,
        marketplace_id: str | None = None,
        transaction_status: str | None = None,
        related_identifier_name: str | None = None,
        related_identifier_value: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[finances_v2024_06_19.Transaction]:
        """every item of ``payload.transactions`` across pages of ``list_transactions``, following ``payload.nextToken`` -> ``next_token``."""
        return apaginate(
            self.list_transactions,
            {
                "posted_after": posted_after,
                "posted_before": posted_before,
                "marketplace_id": marketplace_id,
                "transaction_status": transaction_status,
                "related_identifier_name": related_identifier_name,
                "related_identifier_value": related_identifier_value,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payload", "transactions"),
            token=("payload", "next_token"),
            token_param="next_token",
            wire_items=("payload", "transactions"),
            wire_token=("payload", "nextToken"),
        )

    async def list_balances(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        balance_type: str | None = None,
        account_type: str | None = None,
        as_of_date: datetime.date | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v2024_06_19.ListBalancesResponse:
        """Retrieve a balance. This balance can be a past balances at a specified date, or a current balance. Sub-balances are listed by account type and marketplace.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 10 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that apply to the operation, when available. The preceding table indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        GET /finances/2024-06-19/balances
        """
        params: dict[str, Any] = {
            "marketplaceIds": joined(marketplace_ids, ","),
            "balanceType": balance_type,
            "accountType": account_type,
            "asOfDate": as_of_date,
            "nextToken": next_token,
        }
        return await self._http.request(
            "GET",
            "/finances/2024-06-19/balances",
            operation="listBalances",
            service=SERVICE,
            params=params,
            response=finances_v2024_06_19.ListBalancesResponse,
            error=finances_v2024_06_19.ErrorList,
            rate_limit=RateLimit(0.5, 10),
            options=request_options,
        )

    def iter_list_balances(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        balance_type: str | None = None,
        account_type: str | None = None,
        as_of_date: datetime.date | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[finances_v2024_06_19.Balance]:
        """every item of ``balances`` across pages of ``list_balances``, following ``nextToken`` -> ``next_token``."""
        return apaginate(
            self.list_balances,
            {
                "marketplace_ids": marketplace_ids,
                "balance_type": balance_type,
                "account_type": account_type,
                "as_of_date": as_of_date,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("balances",),
            token=("next_token",),
            token_param="next_token",
            wire_items=("balances",),
            wire_token=("nextToken",),
        )

    async def list_summary(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        account_type: str | None = None,
        related_identifier_name: str | None = None,
        related_identifier_value: str | None = None,
        period_start: datetime.date | None = None,
        period_end: datetime.date | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> finances_v2024_06_19.SummaryResponse:
        """Retrieve the financial summary for the specified time period or settlement period.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 10 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that apply to the operation, when available. The preceding table indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        GET /finances/2024-06-19/summary
        """
        params: dict[str, Any] = {
            "marketplaceIds": joined(marketplace_ids, ","),
            "accountType": account_type,
            "relatedIdentifierName": related_identifier_name,
            "relatedIdentifierValue": related_identifier_value,
            "periodStart": period_start,
            "periodEnd": period_end,
            "nextToken": next_token,
        }
        return await self._http.request(
            "GET",
            "/finances/2024-06-19/summary",
            operation="listSummary",
            service=SERVICE,
            params=params,
            response=finances_v2024_06_19.SummaryResponse,
            error=finances_v2024_06_19.ErrorList,
            rate_limit=RateLimit(0.5, 10),
            options=request_options,
        )

    def iter_list_summary(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        account_type: str | None = None,
        related_identifier_name: str | None = None,
        related_identifier_value: str | None = None,
        period_start: datetime.date | None = None,
        period_end: datetime.date | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[finances_v2024_06_19.Summary]:
        """every item of ``summaries`` across pages of ``list_summary``, following ``nextToken`` -> ``next_token``."""
        return apaginate(
            self.list_summary,
            {
                "marketplace_ids": marketplace_ids,
                "account_type": account_type,
                "related_identifier_name": related_identifier_name,
                "related_identifier_value": related_identifier_value,
                "period_start": period_start,
                "period_end": period_end,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("summaries",),
            token=("next_token",),
            token_param="next_token",
            wire_items=("summaries",),
            wire_token=("nextToken",),
        )


__all__ = ["AsyncFinancesV20240619Resource", "FinancesV20240619Resource"]
