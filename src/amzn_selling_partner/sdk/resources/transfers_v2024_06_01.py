# Auto-generated by oagen. Do not edit.

"""``TransfersV20240601`` resource of Selling Partner API.

Auto-generated by oagen from the OpenAPI spec; do not edit by hand.
"""

from __future__ import annotations

import datetime
from collections.abc import AsyncIterator, Iterator, Mapping
from typing import Any

from .._http import AsyncHttpClient, HttpClient, RateLimit, RequestOptions, apaginate, joined, paginate
from ..models import transfers_v2024_06_01

SERVICE = "transfers_v2024_06_01"


class TransfersV20240601Resource:
    """Synchronous ``TransfersV20240601`` resource."""

    __slots__ = ("_http",)

    def __init__(self, http: HttpClient) -> None:
        self._http = http

    def list_payouts(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        created_after: datetime.datetime | None = None,
        created_before: datetime.datetime | None = None,
        payout_id: str | None = None,
        account_type: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> transfers_v2024_06_01.ListPayoutsResponse:
        """Retrieve a list of payouts for the selling partner's account. You can filter results by `marketplaceIds`, `accountType`, date range (`createdAfter` and `createdBefore`), or a specific `payoutId`. By default, the response includes payouts for all available marketplaces and account types. Results are grouped by the seller's account groups. Within each account group results are sorted by their creation date, with the most recent appearing first.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 10 |

        The `x-amzn-RateLimit-Limit` response header contains the usage plan rate limits for the operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput might have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        GET /finances/transfers/2024-06-01/payouts
        """
        params: dict[str, Any] = {
            "marketplaceIds": joined(marketplace_ids, ","),
            "createdAfter": created_after,
            "createdBefore": created_before,
            "payoutId": payout_id,
            "accountType": account_type,
            "nextToken": next_token,
        }
        return self._http.request(
            "GET",
            "/finances/transfers/2024-06-01/payouts",
            operation="listPayouts",
            service=SERVICE,
            params=params,
            response=transfers_v2024_06_01.ListPayoutsResponse,
            error=transfers_v2024_06_01.ErrorList,
            rate_limit=RateLimit(0.5, 10),
            options=request_options,
        )

    def iter_list_payouts(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        created_after: datetime.datetime | None = None,
        created_before: datetime.datetime | None = None,
        payout_id: str | None = None,
        account_type: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> Iterator[transfers_v2024_06_01.Payout]:
        """every item of ``payouts`` across pages of ``list_payouts``, following ``nextToken`` -> ``next_token``."""
        return paginate(
            self.list_payouts,
            {
                "marketplace_ids": marketplace_ids,
                "created_after": created_after,
                "created_before": created_before,
                "payout_id": payout_id,
                "account_type": account_type,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payouts",),
            token=("next_token",),
            token_param="next_token",  # nosec B106
            wire_items=("payouts",),
            wire_token=("nextToken",),
        )

    def create_payout(
        self,
        body: transfers_v2024_06_01.InitiatePayoutRequest | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> transfers_v2024_06_01.InitiatePayoutResponse:
        """Initiates an on-demand payout to the seller's default deposit method in Seller Central for the given `marketplaceId` and `accountType`, if eligible. You can only initiate one on-demand payout for each marketplace and account type within a 24-hour period.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.017 | 2 |

        The `x-amzn-RateLimit-Limit` response header contains the usage plan rate limits for the operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput might have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        POST /finances/transfers/2024-06-01/payouts
        """
        return self._http.request(
            "POST",
            "/finances/transfers/2024-06-01/payouts",
            operation="initiatePayout",
            service=SERVICE,
            json=body,
            response=transfers_v2024_06_01.InitiatePayoutResponse,
            error=transfers_v2024_06_01.ErrorList,
            rate_limit=RateLimit(0.017, 2),
            options=request_options,
        )

    def list_payment_methods(
        self,
        marketplace_id: str,
        *,
        payment_method_types: list[transfers_v2024_06_01.TransfersV20240601PaymentMethodTypes] | None = None,
        request_options: RequestOptions | None = None,
    ) -> transfers_v2024_06_01.GetPaymentMethodsResponse:
        """Returns the list of payment methods for the seller, which can be filtered by method type.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | .5 | 30 |

        The `x-amzn-RateLimit-Limit` response header contains the usage plan rate limits for the operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput might have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        GET /finances/transfers/2024-06-01/paymentMethods
        """
        params: dict[str, Any] = {"marketplaceId": marketplace_id, "paymentMethodTypes": joined(payment_method_types, ",")}
        return self._http.request(
            "GET",
            "/finances/transfers/2024-06-01/paymentMethods",
            operation="getPaymentMethods",
            service=SERVICE,
            params=params,
            response=transfers_v2024_06_01.GetPaymentMethodsResponse,
            error=transfers_v2024_06_01.ErrorList,
            rate_limit=RateLimit(0.5, 30),
            options=request_options,
        )

    def list_expected(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        account_type: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> transfers_v2024_06_01.ListExpectedPayoutsResponse:
        """Returns the upcoming expected payouts from Amazon associated with a partner's account for the specified parameters.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 10 |

        The `x-amzn-RateLimit-Limit` response header contains the usage plan rate limits for the operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput might have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        GET /finances/transfers/2024-06-01/payouts/expected
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ","), "accountType": account_type, "nextToken": next_token}
        return self._http.request(
            "GET",
            "/finances/transfers/2024-06-01/payouts/expected",
            operation="listExpectedPayouts",
            service=SERVICE,
            params=params,
            response=transfers_v2024_06_01.ListExpectedPayoutsResponse,
            error=transfers_v2024_06_01.ErrorList,
            rate_limit=RateLimit(0.5, 10),
            options=request_options,
        )

    def iter_list_expected(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        account_type: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> Iterator[transfers_v2024_06_01.ExpectedPayout]:
        """every item of ``expectedPayouts`` across pages of ``list_expected``, following ``nextToken`` -> ``next_token``."""
        return paginate(
            self.list_expected,
            {
                "marketplace_ids": marketplace_ids,
                "account_type": account_type,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("expected_payouts",),
            token=("next_token",),
            token_param="next_token",  # nosec B106
            wire_items=("expectedPayouts",),
            wire_token=("nextToken",),
        )


class AsyncTransfersV20240601Resource:
    """Asynchronous ``TransfersV20240601`` resource."""

    __slots__ = ("_http",)

    def __init__(self, http: AsyncHttpClient) -> None:
        self._http = http

    async def list_payouts(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        created_after: datetime.datetime | None = None,
        created_before: datetime.datetime | None = None,
        payout_id: str | None = None,
        account_type: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> transfers_v2024_06_01.ListPayoutsResponse:
        """Retrieve a list of payouts for the selling partner's account. You can filter results by `marketplaceIds`, `accountType`, date range (`createdAfter` and `createdBefore`), or a specific `payoutId`. By default, the response includes payouts for all available marketplaces and account types. Results are grouped by the seller's account groups. Within each account group results are sorted by their creation date, with the most recent appearing first.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 10 |

        The `x-amzn-RateLimit-Limit` response header contains the usage plan rate limits for the operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput might have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        GET /finances/transfers/2024-06-01/payouts
        """
        params: dict[str, Any] = {
            "marketplaceIds": joined(marketplace_ids, ","),
            "createdAfter": created_after,
            "createdBefore": created_before,
            "payoutId": payout_id,
            "accountType": account_type,
            "nextToken": next_token,
        }
        return await self._http.request(
            "GET",
            "/finances/transfers/2024-06-01/payouts",
            operation="listPayouts",
            service=SERVICE,
            params=params,
            response=transfers_v2024_06_01.ListPayoutsResponse,
            error=transfers_v2024_06_01.ErrorList,
            rate_limit=RateLimit(0.5, 10),
            options=request_options,
        )

    def iter_list_payouts(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        created_after: datetime.datetime | None = None,
        created_before: datetime.datetime | None = None,
        payout_id: str | None = None,
        account_type: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[transfers_v2024_06_01.Payout]:
        """every item of ``payouts`` across pages of ``list_payouts``, following ``nextToken`` -> ``next_token``."""
        return apaginate(
            self.list_payouts,
            {
                "marketplace_ids": marketplace_ids,
                "created_after": created_after,
                "created_before": created_before,
                "payout_id": payout_id,
                "account_type": account_type,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("payouts",),
            token=("next_token",),
            token_param="next_token",  # nosec B106
            wire_items=("payouts",),
            wire_token=("nextToken",),
        )

    async def create_payout(
        self,
        body: transfers_v2024_06_01.InitiatePayoutRequest | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> transfers_v2024_06_01.InitiatePayoutResponse:
        """Initiates an on-demand payout to the seller's default deposit method in Seller Central for the given `marketplaceId` and `accountType`, if eligible. You can only initiate one on-demand payout for each marketplace and account type within a 24-hour period.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.017 | 2 |

        The `x-amzn-RateLimit-Limit` response header contains the usage plan rate limits for the operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput might have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        POST /finances/transfers/2024-06-01/payouts
        """
        return await self._http.request(
            "POST",
            "/finances/transfers/2024-06-01/payouts",
            operation="initiatePayout",
            service=SERVICE,
            json=body,
            response=transfers_v2024_06_01.InitiatePayoutResponse,
            error=transfers_v2024_06_01.ErrorList,
            rate_limit=RateLimit(0.017, 2),
            options=request_options,
        )

    async def list_payment_methods(
        self,
        marketplace_id: str,
        *,
        payment_method_types: list[transfers_v2024_06_01.TransfersV20240601PaymentMethodTypes] | None = None,
        request_options: RequestOptions | None = None,
    ) -> transfers_v2024_06_01.GetPaymentMethodsResponse:
        """Returns the list of payment methods for the seller, which can be filtered by method type.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | .5 | 30 |

        The `x-amzn-RateLimit-Limit` response header contains the usage plan rate limits for the operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput might have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        GET /finances/transfers/2024-06-01/paymentMethods
        """
        params: dict[str, Any] = {"marketplaceId": marketplace_id, "paymentMethodTypes": joined(payment_method_types, ",")}
        return await self._http.request(
            "GET",
            "/finances/transfers/2024-06-01/paymentMethods",
            operation="getPaymentMethods",
            service=SERVICE,
            params=params,
            response=transfers_v2024_06_01.GetPaymentMethodsResponse,
            error=transfers_v2024_06_01.ErrorList,
            rate_limit=RateLimit(0.5, 30),
            options=request_options,
        )

    async def list_expected(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        account_type: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> transfers_v2024_06_01.ListExpectedPayoutsResponse:
        """Returns the upcoming expected payouts from Amazon associated with a partner's account for the specified parameters.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 10 |

        The `x-amzn-RateLimit-Limit` response header contains the usage plan rate limits for the operation, when available. The preceding table contains the default rate and burst values for this operation. Selling partners whose business demands require higher throughput might have higher rate and burst values than those shown here. For more information, refer to [Usage Plans and Rate Limits](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        GET /finances/transfers/2024-06-01/payouts/expected
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ","), "accountType": account_type, "nextToken": next_token}
        return await self._http.request(
            "GET",
            "/finances/transfers/2024-06-01/payouts/expected",
            operation="listExpectedPayouts",
            service=SERVICE,
            params=params,
            response=transfers_v2024_06_01.ListExpectedPayoutsResponse,
            error=transfers_v2024_06_01.ErrorList,
            rate_limit=RateLimit(0.5, 10),
            options=request_options,
        )

    def iter_list_expected(
        self,
        *,
        marketplace_ids: list[str] | None = None,
        account_type: str | None = None,
        next_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[transfers_v2024_06_01.ExpectedPayout]:
        """every item of ``expectedPayouts`` across pages of ``list_expected``, following ``nextToken`` -> ``next_token``."""
        return apaginate(
            self.list_expected,
            {
                "marketplace_ids": marketplace_ids,
                "account_type": account_type,
                "next_token": next_token,
                "request_options": request_options,
            },
            items=("expected_payouts",),
            token=("next_token",),
            token_param="next_token",  # nosec B106
            wire_items=("expectedPayouts",),
            wire_token=("nextToken",),
        )


__all__ = ["AsyncTransfersV20240601Resource", "TransfersV20240601Resource"]
