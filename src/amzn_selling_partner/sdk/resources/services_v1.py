# Auto-generated by oagen. Do not edit.

"""``ServicesV1`` resource of Selling Partner API.

Auto-generated by oagen from the OpenAPI spec; do not edit by hand.
"""

from __future__ import annotations

from collections.abc import AsyncIterator, Iterator, Mapping
from typing import Any

from .._http import AsyncHttpClient, HttpClient, RateLimit, RequestOptions, apaginate, joined, paginate, path_segment
from ..models import services_v1

SERVICE = "services_v1"


class ServicesV1Resource:
    """Synchronous ``ServicesV1`` resource."""

    __slots__ = ("_http",)

    def __init__(self, http: HttpClient) -> None:
        self._http = http

    def get_service_job(
        self,
        service_job_id: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.GetServiceJobByServiceJobIdResponse:
        """Gets details of service job indicated by the provided `serviceJobID`.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 20 | 40 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /service/v1/serviceJobs/{serviceJobId}
        """
        return self._http.request(
            "GET",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}",
            operation="getServiceJobByServiceJobId",
            service=SERVICE,
            response=services_v1.GetServiceJobByServiceJobIdResponse,
            error=services_v1.GetServiceJobByServiceJobIdResponse,
            rate_limit=RateLimit(20, 40),
            options=request_options,
        )

    def update_service_job_cancellations(
        self,
        service_job_id: str,
        cancellation_reason_code: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.CancelServiceJobByServiceJobIdResponse:
        """Cancels the service job indicated by the service job identifier specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/serviceJobs/{serviceJobId}/cancellations
        """
        params: dict[str, Any] = {"cancellationReasonCode": cancellation_reason_code}
        return self._http.request(
            "PUT",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/cancellations",
            operation="cancelServiceJobByServiceJobId",
            service=SERVICE,
            params=params,
            response=services_v1.CancelServiceJobByServiceJobIdResponse,
            error=services_v1.CancelServiceJobByServiceJobIdResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def update_service_job_completions(
        self,
        service_job_id: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.CompleteServiceJobByServiceJobIdResponse:
        """Completes the service job indicated by the service job identifier specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/serviceJobs/{serviceJobId}/completions
        """
        return self._http.request(
            "PUT",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/completions",
            operation="completeServiceJobByServiceJobId",
            service=SERVICE,
            response=services_v1.CompleteServiceJobByServiceJobIdResponse,
            error=services_v1.CompleteServiceJobByServiceJobIdResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def list_service_jobs(
        self,
        marketplace_ids: list[str],
        *,
        service_order_ids: list[str] | None = None,
        product_order_ids: list[str] | None = None,
        tracking_ids: list[str] | None = None,
        service_job_status: list[services_v1.ServicesV1ServiceJobStatus] | None = None,
        page_token: str | None = None,
        page_size: int | None = None,
        sort_field: services_v1.ServicesV1SortField | str | None = None,
        sort_order: services_v1.ServicesV1SortOrder | str | None = None,
        created_after: str | None = None,
        created_before: str | None = None,
        last_updated_after: str | None = None,
        last_updated_before: str | None = None,
        schedule_start_date: str | None = None,
        schedule_end_date: str | None = None,
        asins: list[str] | None = None,
        required_skills: list[str] | None = None,
        store_ids: list[str] | None = None,
        request_options: RequestOptions | None = None,
    ) -> services_v1.GetServiceJobsResponse:
        """Gets service job details for the specified filter query.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 10 | 40 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /service/v1/serviceJobs
        """
        params: dict[str, Any] = {
            "serviceOrderIds": joined(service_order_ids, ","),
            "productOrderIds": joined(product_order_ids, ","),
            "trackingIds": joined(tracking_ids, ","),
            "serviceJobStatus": joined(service_job_status, ","),
            "pageToken": page_token,
            "pageSize": page_size,
            "sortField": sort_field,
            "sortOrder": sort_order,
            "createdAfter": created_after,
            "createdBefore": created_before,
            "lastUpdatedAfter": last_updated_after,
            "lastUpdatedBefore": last_updated_before,
            "scheduleStartDate": schedule_start_date,
            "scheduleEndDate": schedule_end_date,
            "marketplaceIds": joined(marketplace_ids, ","),
            "asins": joined(asins, ","),
            "requiredSkills": joined(required_skills, ","),
            "storeIds": joined(store_ids, ","),
        }
        return self._http.request(
            "GET",
            "/service/v1/serviceJobs",
            operation="getServiceJobs",
            service=SERVICE,
            params=params,
            response=services_v1.GetServiceJobsResponse,
            error=services_v1.GetServiceJobsResponse,
            rate_limit=RateLimit(10, 40),
            options=request_options,
        )

    def iter_list_service_jobs(
        self,
        marketplace_ids: list[str],
        *,
        service_order_ids: list[str] | None = None,
        product_order_ids: list[str] | None = None,
        tracking_ids: list[str] | None = None,
        service_job_status: list[services_v1.ServicesV1ServiceJobStatus] | None = None,
        page_token: str | None = None,
        page_size: int | None = None,
        sort_field: services_v1.ServicesV1SortField | str | None = None,
        sort_order: services_v1.ServicesV1SortOrder | str | None = None,
        created_after: str | None = None,
        created_before: str | None = None,
        last_updated_after: str | None = None,
        last_updated_before: str | None = None,
        schedule_start_date: str | None = None,
        schedule_end_date: str | None = None,
        asins: list[str] | None = None,
        required_skills: list[str] | None = None,
        store_ids: list[str] | None = None,
        request_options: RequestOptions | None = None,
    ) -> Iterator[services_v1.ServiceJob]:
        """every item of ``payload.jobs`` across pages of ``list_service_jobs``, following ``payload.nextPageToken`` -> ``page_token``."""
        return paginate(
            self.list_service_jobs,
            {
                "service_order_ids": service_order_ids,
                "product_order_ids": product_order_ids,
                "tracking_ids": tracking_ids,
                "service_job_status": service_job_status,
                "page_token": page_token,
                "page_size": page_size,
                "sort_field": sort_field,
                "sort_order": sort_order,
                "created_after": created_after,
                "created_before": created_before,
                "last_updated_after": last_updated_after,
                "last_updated_before": last_updated_before,
                "schedule_start_date": schedule_start_date,
                "schedule_end_date": schedule_end_date,
                "marketplace_ids": marketplace_ids,
                "asins": asins,
                "required_skills": required_skills,
                "store_ids": store_ids,
                "request_options": request_options,
            },
            items=("payload", "jobs"),
            token=("payload", "next_page_token"),
            token_param="page_token",  # nosec B106
            wire_items=("payload", "jobs"),
            wire_token=("payload", "nextPageToken"),
        )

    def add_appointment_for_service_job_by_service_job_id(
        self,
        service_job_id: str,
        body: services_v1.AddAppointmentRequest | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.SetAppointmentResponse:
        """Adds an appointment to the service job indicated by the service job identifier specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/serviceJobs/{serviceJobId}/appointments
        """
        return self._http.request(
            "POST",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/appointments",
            operation="addAppointmentForServiceJobByServiceJobId",
            service=SERVICE,
            json=body,
            response=services_v1.SetAppointmentResponse,
            error=services_v1.SetAppointmentResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def reschedule_appointment_for_service_job_by_service_job_id(
        self,
        service_job_id: str,
        appointment_id: str,
        body: services_v1.RescheduleAppointmentRequest | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.SetAppointmentResponse:
        """Reschedules an appointment for the service job indicated by the service job identifier specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/serviceJobs/{serviceJobId}/appointments/{appointmentId}
        """
        return self._http.request(
            "POST",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/appointments/{path_segment(appointment_id)}",
            operation="rescheduleAppointmentForServiceJobByServiceJobId",
            service=SERVICE,
            json=body,
            response=services_v1.SetAppointmentResponse,
            error=services_v1.SetAppointmentResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def update_appointment_resources(
        self,
        service_job_id: str,
        appointment_id: str,
        body: services_v1.AssignAppointmentResourcesRequest | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.AssignAppointmentResourcesResponse:
        """Assigns new resource(s) or overwrite/update the existing one(s) to a service job appointment.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 1 | 2 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/serviceJobs/{serviceJobId}/appointments/{appointmentId}/resources
        """
        return self._http.request(
            "PUT",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/appointments/{path_segment(appointment_id)}/resources",
            operation="assignAppointmentResources",
            service=SERVICE,
            json=body,
            response=services_v1.AssignAppointmentResourcesResponse,
            error=services_v1.AssignAppointmentResourcesResponse,
            rate_limit=RateLimit(1, 2),
            options=request_options,
        )

    def update_appointment_fulfillment(
        self,
        service_job_id: str,
        appointment_id: str,
        body: services_v1.SetAppointmentFulfillmentDataRequest | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> str:
        """Updates the appointment fulfillment data related to a given `jobID` and `appointmentID`.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/serviceJobs/{serviceJobId}/appointments/{appointmentId}/fulfillment
        """
        return self._http.request(
            "PUT",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/appointments/{path_segment(appointment_id)}/fulfillment",
            operation="setAppointmentFulfillmentData",
            service=SERVICE,
            json=body,
            response=str,
            error=list[services_v1.Error],
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def create_range(
        self,
        resource_id: str,
        body: services_v1.RangeSlotCapacityQuery | Mapping[str, Any],
        marketplace_ids: list[str],
        *,
        next_page_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> services_v1.RangeSlotCapacity:
        """Provides capacity slots in a format similar to availability records.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/serviceResources/{resourceId}/capacity/range
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ","), "nextPageToken": next_page_token}
        return self._http.request(
            "POST",
            f"/service/v1/serviceResources/{path_segment(resource_id)}/capacity/range",
            operation="getRangeSlotCapacity",
            service=SERVICE,
            params=params,
            json=body,
            response=services_v1.RangeSlotCapacity,
            error=services_v1.RangeSlotCapacityErrors,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def create_fixed(
        self,
        resource_id: str,
        body: services_v1.FixedSlotCapacityQuery | Mapping[str, Any],
        marketplace_ids: list[str],
        *,
        next_page_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> services_v1.FixedSlotCapacity:
        """Provides capacity in fixed-size slots.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/serviceResources/{resourceId}/capacity/fixed
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ","), "nextPageToken": next_page_token}
        return self._http.request(
            "POST",
            f"/service/v1/serviceResources/{path_segment(resource_id)}/capacity/fixed",
            operation="getFixedSlotCapacity",
            service=SERVICE,
            params=params,
            json=body,
            response=services_v1.FixedSlotCapacity,
            error=services_v1.FixedSlotCapacityErrors,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def update_service_resource_schedules(
        self,
        resource_id: str,
        body: services_v1.UpdateScheduleRequest | Mapping[str, Any],
        marketplace_ids: list[str],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.UpdateScheduleResponse:
        """Update the schedule of the given resource.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/serviceResources/{resourceId}/schedules
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ",")}
        return self._http.request(
            "PUT",
            f"/service/v1/serviceResources/{path_segment(resource_id)}/schedules",
            operation="updateSchedule",
            service=SERVICE,
            params=params,
            json=body,
            response=services_v1.UpdateScheduleResponse,
            error=services_v1.UpdateScheduleResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def create_reservation(
        self,
        body: services_v1.CreateReservationRequest | Mapping[str, Any],
        marketplace_ids: list[str],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.CreateReservationResponse:
        """Create a reservation.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/reservation
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ",")}
        return self._http.request(
            "POST",
            "/service/v1/reservation",
            operation="createReservation",
            service=SERVICE,
            params=params,
            json=body,
            response=services_v1.CreateReservationResponse,
            error=services_v1.CreateReservationResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def update_reservation(
        self,
        reservation_id: str,
        body: services_v1.UpdateReservationRequest | Mapping[str, Any],
        marketplace_ids: list[str],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.UpdateReservationResponse:
        """Update a reservation.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/reservation/{reservationId}
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ",")}
        return self._http.request(
            "PUT",
            f"/service/v1/reservation/{path_segment(reservation_id)}",
            operation="updateReservation",
            service=SERVICE,
            params=params,
            json=body,
            response=services_v1.UpdateReservationResponse,
            error=services_v1.UpdateReservationResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def delete_reservation(
        self,
        reservation_id: str,
        marketplace_ids: list[str],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.CancelReservationResponse:
        """Cancel a reservation.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        DELETE /service/v1/reservation/{reservationId}
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ",")}
        return self._http.request(
            "DELETE",
            f"/service/v1/reservation/{path_segment(reservation_id)}",
            operation="cancelReservation",
            service=SERVICE,
            params=params,
            response=services_v1.CancelReservationResponse,
            error=services_v1.CancelReservationResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def list_service_job_appointment_slots(
        self,
        service_job_id: str,
        marketplace_ids: list[str],
        *,
        start_time: str | None = None,
        end_time: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> services_v1.GetAppointmentSlotsResponse:
        """Gets appointment slots for the service associated with the service job id specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /service/v1/serviceJobs/{serviceJobId}/appointmentSlots
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ","), "startTime": start_time, "endTime": end_time}
        return self._http.request(
            "GET",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/appointmentSlots",
            operation="getAppointmmentSlotsByJobId",
            service=SERVICE,
            params=params,
            response=services_v1.GetAppointmentSlotsResponse,
            error=services_v1.GetAppointmentSlotsResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    def list_appointment_slots(
        self,
        asin: str,
        store_id: str,
        marketplace_ids: list[str],
        *,
        start_time: str | None = None,
        end_time: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> services_v1.GetAppointmentSlotsResponse:
        """Gets appointment slots as per the service context specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 20 | 40 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /service/v1/appointmentSlots
        """
        params: dict[str, Any] = {
            "asin": asin,
            "storeId": store_id,
            "marketplaceIds": joined(marketplace_ids, ","),
            "startTime": start_time,
            "endTime": end_time,
        }
        return self._http.request(
            "GET",
            "/service/v1/appointmentSlots",
            operation="getAppointmentSlots",
            service=SERVICE,
            params=params,
            response=services_v1.GetAppointmentSlotsResponse,
            error=services_v1.GetAppointmentSlotsResponse,
            rate_limit=RateLimit(20, 40),
            options=request_options,
        )

    def create_document(
        self,
        body: services_v1.ServiceUploadDocument | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.CreateServiceDocumentUploadDestination:
        """Creates an upload destination.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/documents
        """
        return self._http.request(
            "POST",
            "/service/v1/documents",
            operation="createServiceDocumentUploadDestination",
            service=SERVICE,
            json=body,
            response=services_v1.CreateServiceDocumentUploadDestination,
            error=services_v1.CreateServiceDocumentUploadDestination,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )


class AsyncServicesV1Resource:
    """Asynchronous ``ServicesV1`` resource."""

    __slots__ = ("_http",)

    def __init__(self, http: AsyncHttpClient) -> None:
        self._http = http

    async def get_service_job(
        self,
        service_job_id: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.GetServiceJobByServiceJobIdResponse:
        """Gets details of service job indicated by the provided `serviceJobID`.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 20 | 40 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /service/v1/serviceJobs/{serviceJobId}
        """
        return await self._http.request(
            "GET",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}",
            operation="getServiceJobByServiceJobId",
            service=SERVICE,
            response=services_v1.GetServiceJobByServiceJobIdResponse,
            error=services_v1.GetServiceJobByServiceJobIdResponse,
            rate_limit=RateLimit(20, 40),
            options=request_options,
        )

    async def update_service_job_cancellations(
        self,
        service_job_id: str,
        cancellation_reason_code: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.CancelServiceJobByServiceJobIdResponse:
        """Cancels the service job indicated by the service job identifier specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/serviceJobs/{serviceJobId}/cancellations
        """
        params: dict[str, Any] = {"cancellationReasonCode": cancellation_reason_code}
        return await self._http.request(
            "PUT",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/cancellations",
            operation="cancelServiceJobByServiceJobId",
            service=SERVICE,
            params=params,
            response=services_v1.CancelServiceJobByServiceJobIdResponse,
            error=services_v1.CancelServiceJobByServiceJobIdResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def update_service_job_completions(
        self,
        service_job_id: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.CompleteServiceJobByServiceJobIdResponse:
        """Completes the service job indicated by the service job identifier specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/serviceJobs/{serviceJobId}/completions
        """
        return await self._http.request(
            "PUT",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/completions",
            operation="completeServiceJobByServiceJobId",
            service=SERVICE,
            response=services_v1.CompleteServiceJobByServiceJobIdResponse,
            error=services_v1.CompleteServiceJobByServiceJobIdResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def list_service_jobs(
        self,
        marketplace_ids: list[str],
        *,
        service_order_ids: list[str] | None = None,
        product_order_ids: list[str] | None = None,
        tracking_ids: list[str] | None = None,
        service_job_status: list[services_v1.ServicesV1ServiceJobStatus] | None = None,
        page_token: str | None = None,
        page_size: int | None = None,
        sort_field: services_v1.ServicesV1SortField | str | None = None,
        sort_order: services_v1.ServicesV1SortOrder | str | None = None,
        created_after: str | None = None,
        created_before: str | None = None,
        last_updated_after: str | None = None,
        last_updated_before: str | None = None,
        schedule_start_date: str | None = None,
        schedule_end_date: str | None = None,
        asins: list[str] | None = None,
        required_skills: list[str] | None = None,
        store_ids: list[str] | None = None,
        request_options: RequestOptions | None = None,
    ) -> services_v1.GetServiceJobsResponse:
        """Gets service job details for the specified filter query.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 10 | 40 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /service/v1/serviceJobs
        """
        params: dict[str, Any] = {
            "serviceOrderIds": joined(service_order_ids, ","),
            "productOrderIds": joined(product_order_ids, ","),
            "trackingIds": joined(tracking_ids, ","),
            "serviceJobStatus": joined(service_job_status, ","),
            "pageToken": page_token,
            "pageSize": page_size,
            "sortField": sort_field,
            "sortOrder": sort_order,
            "createdAfter": created_after,
            "createdBefore": created_before,
            "lastUpdatedAfter": last_updated_after,
            "lastUpdatedBefore": last_updated_before,
            "scheduleStartDate": schedule_start_date,
            "scheduleEndDate": schedule_end_date,
            "marketplaceIds": joined(marketplace_ids, ","),
            "asins": joined(asins, ","),
            "requiredSkills": joined(required_skills, ","),
            "storeIds": joined(store_ids, ","),
        }
        return await self._http.request(
            "GET",
            "/service/v1/serviceJobs",
            operation="getServiceJobs",
            service=SERVICE,
            params=params,
            response=services_v1.GetServiceJobsResponse,
            error=services_v1.GetServiceJobsResponse,
            rate_limit=RateLimit(10, 40),
            options=request_options,
        )

    def iter_list_service_jobs(
        self,
        marketplace_ids: list[str],
        *,
        service_order_ids: list[str] | None = None,
        product_order_ids: list[str] | None = None,
        tracking_ids: list[str] | None = None,
        service_job_status: list[services_v1.ServicesV1ServiceJobStatus] | None = None,
        page_token: str | None = None,
        page_size: int | None = None,
        sort_field: services_v1.ServicesV1SortField | str | None = None,
        sort_order: services_v1.ServicesV1SortOrder | str | None = None,
        created_after: str | None = None,
        created_before: str | None = None,
        last_updated_after: str | None = None,
        last_updated_before: str | None = None,
        schedule_start_date: str | None = None,
        schedule_end_date: str | None = None,
        asins: list[str] | None = None,
        required_skills: list[str] | None = None,
        store_ids: list[str] | None = None,
        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[services_v1.ServiceJob]:
        """every item of ``payload.jobs`` across pages of ``list_service_jobs``, following ``payload.nextPageToken`` -> ``page_token``."""
        return apaginate(
            self.list_service_jobs,
            {
                "service_order_ids": service_order_ids,
                "product_order_ids": product_order_ids,
                "tracking_ids": tracking_ids,
                "service_job_status": service_job_status,
                "page_token": page_token,
                "page_size": page_size,
                "sort_field": sort_field,
                "sort_order": sort_order,
                "created_after": created_after,
                "created_before": created_before,
                "last_updated_after": last_updated_after,
                "last_updated_before": last_updated_before,
                "schedule_start_date": schedule_start_date,
                "schedule_end_date": schedule_end_date,
                "marketplace_ids": marketplace_ids,
                "asins": asins,
                "required_skills": required_skills,
                "store_ids": store_ids,
                "request_options": request_options,
            },
            items=("payload", "jobs"),
            token=("payload", "next_page_token"),
            token_param="page_token",  # nosec B106
            wire_items=("payload", "jobs"),
            wire_token=("payload", "nextPageToken"),
        )

    async def add_appointment_for_service_job_by_service_job_id(
        self,
        service_job_id: str,
        body: services_v1.AddAppointmentRequest | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.SetAppointmentResponse:
        """Adds an appointment to the service job indicated by the service job identifier specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/serviceJobs/{serviceJobId}/appointments
        """
        return await self._http.request(
            "POST",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/appointments",
            operation="addAppointmentForServiceJobByServiceJobId",
            service=SERVICE,
            json=body,
            response=services_v1.SetAppointmentResponse,
            error=services_v1.SetAppointmentResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def reschedule_appointment_for_service_job_by_service_job_id(
        self,
        service_job_id: str,
        appointment_id: str,
        body: services_v1.RescheduleAppointmentRequest | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.SetAppointmentResponse:
        """Reschedules an appointment for the service job indicated by the service job identifier specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/serviceJobs/{serviceJobId}/appointments/{appointmentId}
        """
        return await self._http.request(
            "POST",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/appointments/{path_segment(appointment_id)}",
            operation="rescheduleAppointmentForServiceJobByServiceJobId",
            service=SERVICE,
            json=body,
            response=services_v1.SetAppointmentResponse,
            error=services_v1.SetAppointmentResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def update_appointment_resources(
        self,
        service_job_id: str,
        appointment_id: str,
        body: services_v1.AssignAppointmentResourcesRequest | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.AssignAppointmentResourcesResponse:
        """Assigns new resource(s) or overwrite/update the existing one(s) to a service job appointment.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 1 | 2 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/serviceJobs/{serviceJobId}/appointments/{appointmentId}/resources
        """
        return await self._http.request(
            "PUT",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/appointments/{path_segment(appointment_id)}/resources",
            operation="assignAppointmentResources",
            service=SERVICE,
            json=body,
            response=services_v1.AssignAppointmentResourcesResponse,
            error=services_v1.AssignAppointmentResourcesResponse,
            rate_limit=RateLimit(1, 2),
            options=request_options,
        )

    async def update_appointment_fulfillment(
        self,
        service_job_id: str,
        appointment_id: str,
        body: services_v1.SetAppointmentFulfillmentDataRequest | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> str:
        """Updates the appointment fulfillment data related to a given `jobID` and `appointmentID`.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/serviceJobs/{serviceJobId}/appointments/{appointmentId}/fulfillment
        """
        return await self._http.request(
            "PUT",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/appointments/{path_segment(appointment_id)}/fulfillment",
            operation="setAppointmentFulfillmentData",
            service=SERVICE,
            json=body,
            response=str,
            error=list[services_v1.Error],
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def create_range(
        self,
        resource_id: str,
        body: services_v1.RangeSlotCapacityQuery | Mapping[str, Any],
        marketplace_ids: list[str],
        *,
        next_page_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> services_v1.RangeSlotCapacity:
        """Provides capacity slots in a format similar to availability records.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/serviceResources/{resourceId}/capacity/range
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ","), "nextPageToken": next_page_token}
        return await self._http.request(
            "POST",
            f"/service/v1/serviceResources/{path_segment(resource_id)}/capacity/range",
            operation="getRangeSlotCapacity",
            service=SERVICE,
            params=params,
            json=body,
            response=services_v1.RangeSlotCapacity,
            error=services_v1.RangeSlotCapacityErrors,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def create_fixed(
        self,
        resource_id: str,
        body: services_v1.FixedSlotCapacityQuery | Mapping[str, Any],
        marketplace_ids: list[str],
        *,
        next_page_token: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> services_v1.FixedSlotCapacity:
        """Provides capacity in fixed-size slots.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/serviceResources/{resourceId}/capacity/fixed
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ","), "nextPageToken": next_page_token}
        return await self._http.request(
            "POST",
            f"/service/v1/serviceResources/{path_segment(resource_id)}/capacity/fixed",
            operation="getFixedSlotCapacity",
            service=SERVICE,
            params=params,
            json=body,
            response=services_v1.FixedSlotCapacity,
            error=services_v1.FixedSlotCapacityErrors,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def update_service_resource_schedules(
        self,
        resource_id: str,
        body: services_v1.UpdateScheduleRequest | Mapping[str, Any],
        marketplace_ids: list[str],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.UpdateScheduleResponse:
        """Update the schedule of the given resource.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/serviceResources/{resourceId}/schedules
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ",")}
        return await self._http.request(
            "PUT",
            f"/service/v1/serviceResources/{path_segment(resource_id)}/schedules",
            operation="updateSchedule",
            service=SERVICE,
            params=params,
            json=body,
            response=services_v1.UpdateScheduleResponse,
            error=services_v1.UpdateScheduleResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def create_reservation(
        self,
        body: services_v1.CreateReservationRequest | Mapping[str, Any],
        marketplace_ids: list[str],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.CreateReservationResponse:
        """Create a reservation.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/reservation
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ",")}
        return await self._http.request(
            "POST",
            "/service/v1/reservation",
            operation="createReservation",
            service=SERVICE,
            params=params,
            json=body,
            response=services_v1.CreateReservationResponse,
            error=services_v1.CreateReservationResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def update_reservation(
        self,
        reservation_id: str,
        body: services_v1.UpdateReservationRequest | Mapping[str, Any],
        marketplace_ids: list[str],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.UpdateReservationResponse:
        """Update a reservation.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        PUT /service/v1/reservation/{reservationId}
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ",")}
        return await self._http.request(
            "PUT",
            f"/service/v1/reservation/{path_segment(reservation_id)}",
            operation="updateReservation",
            service=SERVICE,
            params=params,
            json=body,
            response=services_v1.UpdateReservationResponse,
            error=services_v1.UpdateReservationResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def delete_reservation(
        self,
        reservation_id: str,
        marketplace_ids: list[str],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.CancelReservationResponse:
        """Cancel a reservation.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        DELETE /service/v1/reservation/{reservationId}
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ",")}
        return await self._http.request(
            "DELETE",
            f"/service/v1/reservation/{path_segment(reservation_id)}",
            operation="cancelReservation",
            service=SERVICE,
            params=params,
            response=services_v1.CancelReservationResponse,
            error=services_v1.CancelReservationResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def list_service_job_appointment_slots(
        self,
        service_job_id: str,
        marketplace_ids: list[str],
        *,
        start_time: str | None = None,
        end_time: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> services_v1.GetAppointmentSlotsResponse:
        """Gets appointment slots for the service associated with the service job id specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /service/v1/serviceJobs/{serviceJobId}/appointmentSlots
        """
        params: dict[str, Any] = {"marketplaceIds": joined(marketplace_ids, ","), "startTime": start_time, "endTime": end_time}
        return await self._http.request(
            "GET",
            f"/service/v1/serviceJobs/{path_segment(service_job_id)}/appointmentSlots",
            operation="getAppointmmentSlotsByJobId",
            service=SERVICE,
            params=params,
            response=services_v1.GetAppointmentSlotsResponse,
            error=services_v1.GetAppointmentSlotsResponse,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )

    async def list_appointment_slots(
        self,
        asin: str,
        store_id: str,
        marketplace_ids: list[str],
        *,
        start_time: str | None = None,
        end_time: str | None = None,
        request_options: RequestOptions | None = None,
    ) -> services_v1.GetAppointmentSlotsResponse:
        """Gets appointment slots as per the service context specified.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 20 | 40 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /service/v1/appointmentSlots
        """
        params: dict[str, Any] = {
            "asin": asin,
            "storeId": store_id,
            "marketplaceIds": joined(marketplace_ids, ","),
            "startTime": start_time,
            "endTime": end_time,
        }
        return await self._http.request(
            "GET",
            "/service/v1/appointmentSlots",
            operation="getAppointmentSlots",
            service=SERVICE,
            params=params,
            response=services_v1.GetAppointmentSlotsResponse,
            error=services_v1.GetAppointmentSlotsResponse,
            rate_limit=RateLimit(20, 40),
            options=request_options,
        )

    async def create_document(
        self,
        body: services_v1.ServiceUploadDocument | Mapping[str, Any],
        *,
        request_options: RequestOptions | None = None,
    ) -> services_v1.CreateServiceDocumentUploadDestination:
        """Creates an upload destination.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 5 | 20 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /service/v1/documents
        """
        return await self._http.request(
            "POST",
            "/service/v1/documents",
            operation="createServiceDocumentUploadDestination",
            service=SERVICE,
            json=body,
            response=services_v1.CreateServiceDocumentUploadDestination,
            error=services_v1.CreateServiceDocumentUploadDestination,
            rate_limit=RateLimit(5, 20),
            options=request_options,
        )


__all__ = ["AsyncServicesV1Resource", "ServicesV1Resource"]
