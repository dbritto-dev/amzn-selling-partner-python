# Auto-generated by oagen. Do not edit.

"""``ProductPricingV0`` resource of Selling Partner API.

Auto-generated by oagen from the OpenAPI spec; do not edit by hand.
"""

from __future__ import annotations

from collections.abc import Mapping
from typing import Any

from ..http_client import AsyncHttpClient, HttpClient, RateLimit, RequestOptions, joined, path_segment
from ..models import product_pricing_v0

SERVICE = "product_pricing_v0"


class ProductPricingV0Client:
    """Synchronous ``ProductPricingV0`` resource."""

    __slots__ = ("_client",)

    def __init__(self, client: HttpClient) -> None:
        self._client = client

    def list_price(
        self,
        *,
        marketplace_id: str,
        item_type: product_pricing_v0.ProductPricingV0ItemType | str,
        asins: list[str] | None = None,
        skus: list[str] | None = None,
        item_condition: product_pricing_v0.ProductPricingV0ItemCondition | str | None = None,
        offer_type: product_pricing_v0.ProductPricingV0OfferType | str | None = None,
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetPricingResponse:
        """Returns pricing information for a seller's offer listings based on seller SKU or ASIN.

        **Note:** The parameters associated with this operation may contain special characters that require URL encoding to call the API. To avoid errors with SKUs when encoding URLs, refer to [URL Encoding](https://developer-docs.amazon.com/sp-api/docs/url-encoding).

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 1 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /products/pricing/v0/price
        """
        params: dict[str, Any] = {
            "MarketplaceId": marketplace_id,
            "Asins": joined(asins, ","),
            "Skus": joined(skus, ","),
            "ItemType": item_type,
            "ItemCondition": item_condition,
            "OfferType": offer_type,
        }
        return self._client.request(
            "GET",
            "/products/pricing/v0/price",
            operation="getPricing",
            service=SERVICE,
            params=params,
            response=product_pricing_v0.GetPricingResponse,
            error=product_pricing_v0.GetPricingResponse,
            rate_limit=RateLimit(0.5, 1),
            options=request_options,
        )

    def list_competitive_price(
        self,
        *,
        marketplace_id: str,
        item_type: product_pricing_v0.ProductPricingV0ItemType | str,
        asins: list[str] | None = None,
        skus: list[str] | None = None,
        customer_type: product_pricing_v0.ProductPricingV0CustomerType | str | None = None,
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetPricingResponse:
        """Returns competitive pricing information for a seller's offer listings based on seller SKU or ASIN.

        **Note:** The parameters associated with this operation may contain special characters that require URL encoding to call the API. To avoid errors with SKUs when encoding URLs, refer to [URL Encoding](https://developer-docs.amazon.com/sp-api/docs/url-encoding).

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 1 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /products/pricing/v0/competitivePrice
        """
        params: dict[str, Any] = {
            "MarketplaceId": marketplace_id,
            "Asins": joined(asins, ","),
            "Skus": joined(skus, ","),
            "ItemType": item_type,
            "CustomerType": customer_type,
        }
        return self._client.request(
            "GET",
            "/products/pricing/v0/competitivePrice",
            operation="getCompetitivePricing",
            service=SERVICE,
            params=params,
            response=product_pricing_v0.GetPricingResponse,
            error=product_pricing_v0.GetPricingResponse,
            rate_limit=RateLimit(0.5, 1),
            options=request_options,
        )

    def list_listing_offers(
        self,
        *,
        seller_sku: str,
        marketplace_id: str,
        item_condition: product_pricing_v0.ProductPricingV0ItemCondition | str,
        customer_type: product_pricing_v0.ProductPricingV0CustomerType | str | None = None,
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetOffersResponse:
        """Returns the lowest priced offers for a single SKU listing.

        **Note:** The parameters associated with this operation may contain special characters that require URL encoding to call the API. To avoid errors with SKUs when encoding URLs, refer to [URL Encoding](https://developer-docs.amazon.com/sp-api/docs/url-encoding).

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 1 | 2 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /products/pricing/v0/listings/{SellerSKU}/offers
        """
        params: dict[str, Any] = {"MarketplaceId": marketplace_id, "ItemCondition": item_condition, "CustomerType": customer_type}
        return self._client.request(
            "GET",
            f"/products/pricing/v0/listings/{path_segment(seller_sku)}/offers",
            operation="getListingOffers",
            service=SERVICE,
            params=params,
            response=product_pricing_v0.GetOffersResponse,
            error=product_pricing_v0.GetOffersResponse,
            rate_limit=RateLimit(1, 2),
            options=request_options,
        )

    def list_item_offers(
        self,
        *,
        asin: str,
        marketplace_id: str,
        item_condition: product_pricing_v0.ProductPricingV0ItemCondition | str,
        customer_type: product_pricing_v0.ProductPricingV0CustomerType | str | None = None,
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetOffersResponse:
        """Returns the lowest priced offers for a single item based on ASIN.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 1 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /products/pricing/v0/items/{Asin}/offers
        """
        params: dict[str, Any] = {"MarketplaceId": marketplace_id, "ItemCondition": item_condition, "CustomerType": customer_type}
        return self._client.request(
            "GET",
            f"/products/pricing/v0/items/{path_segment(asin)}/offers",
            operation="getItemOffers",
            service=SERVICE,
            params=params,
            response=product_pricing_v0.GetOffersResponse,
            error=product_pricing_v0.GetOffersResponse,
            rate_limit=RateLimit(0.5, 1),
            options=request_options,
        )

    def create_item_offer(
        self,
        *,
        body: product_pricing_v0.GetItemOffersBatchRequest | Mapping[str, Any],
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetItemOffersBatchResponse:
        """Returns the lowest priced offers for a batch of items based on ASIN.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.1 | 1 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        POST /batches/products/pricing/v0/itemOffers
        """
        return self._client.request(
            "POST",
            "/batches/products/pricing/v0/itemOffers",
            operation="getItemOffersBatch",
            service=SERVICE,
            json=body,
            response=product_pricing_v0.GetItemOffersBatchResponse,
            error=product_pricing_v0.Errors,
            rate_limit=RateLimit(0.1, 1),
            options=request_options,
        )

    def create_listing_offer(
        self,
        *,
        body: product_pricing_v0.GetListingOffersBatchRequest | Mapping[str, Any],
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetListingOffersBatchResponse:
        """Returns the lowest priced offers for a batch of listings by SKU.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 1 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /batches/products/pricing/v0/listingOffers
        """
        return self._client.request(
            "POST",
            "/batches/products/pricing/v0/listingOffers",
            operation="getListingOffersBatch",
            service=SERVICE,
            json=body,
            response=product_pricing_v0.GetListingOffersBatchResponse,
            error=product_pricing_v0.Errors,
            rate_limit=RateLimit(0.5, 1),
            options=request_options,
        )


class AsyncProductPricingV0Client:
    """Asynchronous ``ProductPricingV0`` resource."""

    __slots__ = ("_client",)

    def __init__(self, client: AsyncHttpClient) -> None:
        self._client = client

    async def list_price(
        self,
        *,
        marketplace_id: str,
        item_type: product_pricing_v0.ProductPricingV0ItemType | str,
        asins: list[str] | None = None,
        skus: list[str] | None = None,
        item_condition: product_pricing_v0.ProductPricingV0ItemCondition | str | None = None,
        offer_type: product_pricing_v0.ProductPricingV0OfferType | str | None = None,
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetPricingResponse:
        """Returns pricing information for a seller's offer listings based on seller SKU or ASIN.

        **Note:** The parameters associated with this operation may contain special characters that require URL encoding to call the API. To avoid errors with SKUs when encoding URLs, refer to [URL Encoding](https://developer-docs.amazon.com/sp-api/docs/url-encoding).

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 1 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /products/pricing/v0/price
        """
        params: dict[str, Any] = {
            "MarketplaceId": marketplace_id,
            "Asins": joined(asins, ","),
            "Skus": joined(skus, ","),
            "ItemType": item_type,
            "ItemCondition": item_condition,
            "OfferType": offer_type,
        }
        return await self._client.request(
            "GET",
            "/products/pricing/v0/price",
            operation="getPricing",
            service=SERVICE,
            params=params,
            response=product_pricing_v0.GetPricingResponse,
            error=product_pricing_v0.GetPricingResponse,
            rate_limit=RateLimit(0.5, 1),
            options=request_options,
        )

    async def list_competitive_price(
        self,
        *,
        marketplace_id: str,
        item_type: product_pricing_v0.ProductPricingV0ItemType | str,
        asins: list[str] | None = None,
        skus: list[str] | None = None,
        customer_type: product_pricing_v0.ProductPricingV0CustomerType | str | None = None,
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetPricingResponse:
        """Returns competitive pricing information for a seller's offer listings based on seller SKU or ASIN.

        **Note:** The parameters associated with this operation may contain special characters that require URL encoding to call the API. To avoid errors with SKUs when encoding URLs, refer to [URL Encoding](https://developer-docs.amazon.com/sp-api/docs/url-encoding).

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 1 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /products/pricing/v0/competitivePrice
        """
        params: dict[str, Any] = {
            "MarketplaceId": marketplace_id,
            "Asins": joined(asins, ","),
            "Skus": joined(skus, ","),
            "ItemType": item_type,
            "CustomerType": customer_type,
        }
        return await self._client.request(
            "GET",
            "/products/pricing/v0/competitivePrice",
            operation="getCompetitivePricing",
            service=SERVICE,
            params=params,
            response=product_pricing_v0.GetPricingResponse,
            error=product_pricing_v0.GetPricingResponse,
            rate_limit=RateLimit(0.5, 1),
            options=request_options,
        )

    async def list_listing_offers(
        self,
        *,
        seller_sku: str,
        marketplace_id: str,
        item_condition: product_pricing_v0.ProductPricingV0ItemCondition | str,
        customer_type: product_pricing_v0.ProductPricingV0CustomerType | str | None = None,
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetOffersResponse:
        """Returns the lowest priced offers for a single SKU listing.

        **Note:** The parameters associated with this operation may contain special characters that require URL encoding to call the API. To avoid errors with SKUs when encoding URLs, refer to [URL Encoding](https://developer-docs.amazon.com/sp-api/docs/url-encoding).

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 1 | 2 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /products/pricing/v0/listings/{SellerSKU}/offers
        """
        params: dict[str, Any] = {"MarketplaceId": marketplace_id, "ItemCondition": item_condition, "CustomerType": customer_type}
        return await self._client.request(
            "GET",
            f"/products/pricing/v0/listings/{path_segment(seller_sku)}/offers",
            operation="getListingOffers",
            service=SERVICE,
            params=params,
            response=product_pricing_v0.GetOffersResponse,
            error=product_pricing_v0.GetOffersResponse,
            rate_limit=RateLimit(1, 2),
            options=request_options,
        )

    async def list_item_offers(
        self,
        *,
        asin: str,
        marketplace_id: str,
        item_condition: product_pricing_v0.ProductPricingV0ItemCondition | str,
        customer_type: product_pricing_v0.ProductPricingV0CustomerType | str | None = None,
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetOffersResponse:
        """Returns the lowest priced offers for a single item based on ASIN.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 1 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        GET /products/pricing/v0/items/{Asin}/offers
        """
        params: dict[str, Any] = {"MarketplaceId": marketplace_id, "ItemCondition": item_condition, "CustomerType": customer_type}
        return await self._client.request(
            "GET",
            f"/products/pricing/v0/items/{path_segment(asin)}/offers",
            operation="getItemOffers",
            service=SERVICE,
            params=params,
            response=product_pricing_v0.GetOffersResponse,
            error=product_pricing_v0.GetOffersResponse,
            rate_limit=RateLimit(0.5, 1),
            options=request_options,
        )

    async def create_item_offer(
        self,
        *,
        body: product_pricing_v0.GetItemOffersBatchRequest | Mapping[str, Any],
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetItemOffersBatchResponse:
        """Returns the lowest priced offers for a batch of items based on ASIN.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.1 | 1 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](https://developer-docs.amazon.com/sp-api/docs/usage-plans-and-rate-limits-in-the-sp-api).

        POST /batches/products/pricing/v0/itemOffers
        """
        return await self._client.request(
            "POST",
            "/batches/products/pricing/v0/itemOffers",
            operation="getItemOffersBatch",
            service=SERVICE,
            json=body,
            response=product_pricing_v0.GetItemOffersBatchResponse,
            error=product_pricing_v0.Errors,
            rate_limit=RateLimit(0.1, 1),
            options=request_options,
        )

    async def create_listing_offer(
        self,
        *,
        body: product_pricing_v0.GetListingOffersBatchRequest | Mapping[str, Any],
        request_options: RequestOptions | None = None,
    ) -> product_pricing_v0.GetListingOffersBatchResponse:
        """Returns the lowest priced offers for a batch of listings by SKU.

        **Usage Plan:**

        | Rate (requests per second) | Burst |
        | ---- | ---- |
        | 0.5 | 1 |

        The `x-amzn-RateLimit-Limit` response header returns the usage plan rate limits that were applied to the requested operation, when available. The table above indicates the default rate and burst values for this operation. Selling partners whose business demands require higher throughput may see higher rate and burst values than those shown here. For more information, see [Usage Plans and Rate Limits in the Selling Partner API](doc:usage-plans-and-rate-limits-in-the-sp-api).

        POST /batches/products/pricing/v0/listingOffers
        """
        return await self._client.request(
            "POST",
            "/batches/products/pricing/v0/listingOffers",
            operation="getListingOffersBatch",
            service=SERVICE,
            json=body,
            response=product_pricing_v0.GetListingOffersBatchResponse,
            error=product_pricing_v0.Errors,
            rate_limit=RateLimit(0.5, 1),
            options=request_options,
        )


__all__ = ["AsyncProductPricingV0Client", "ProductPricingV0Client"]
